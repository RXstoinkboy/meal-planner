// Single export point for all UI components. Views must never import Tamagui
// (or any other UI library) directly — always `import { X } from "@/components"`.
export { Button, Input, Spinner, Text, YStack } from "tamagui";
